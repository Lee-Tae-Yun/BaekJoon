#include <iostream>
using namespace std;



int main(){
    int t,a,b,i=0;
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> t;
    while(i < t){
        i++;
        cin >> a >> b;
        cout << "Case #" << i << ": " <<a+b << "\n";
        
    }
    return 0;
}
