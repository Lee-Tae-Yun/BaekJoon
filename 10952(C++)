#include <iostream>
#include <iomanip>
using namespace std;



int main(){
    int a,b;
    ios::sync_with_stdio(false);
    cin.tie(0);

    while(true){
        cin >> a >> b;
        if(a == 0 && b == 0){
            break;
        }
        cout << a+b <<"\n";
    }
    
    return 0;
}
