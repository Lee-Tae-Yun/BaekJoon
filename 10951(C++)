#include <iostream>
#include <sstream>
using namespace std;



int main(){
    int a,b;
    ios::sync_with_stdio(false);
    cin.tie(0);

    while(true){
         if (!(cin >> a >> b)){
            break;
        }
        cout << a+b <<"\n";
        
    }
    
    return 0;
}
