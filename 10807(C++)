#include <iostream>
#include <sstream>
#include <algorithm> 
using namespace std;



int main(){
    int n,v;
    cin >> n;
    int a[n];
    for(int i = 0; i < n; i++){
        cin >> a[i];
    }
    cin >> v;
    cout << count(a,a+n,v)<<"\n";
    return 0;
}
