#include <iostream>
using namespace std;



int main(){
    int t,a,b;
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> t;
    while(t!=0){
        cin >> a >> b;
        cout << a+b << "\n";
        t--;
    }
    return 0;
}
